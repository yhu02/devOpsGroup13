export const resourceVisuals: Record<
  string,
  { icon?: string; color?: string }
> = {
  VPC: { icon: 'src/assets/vpc.png' }, // custom icon for VPC
  RDS: { icon: 'src/assets/rds.jpg' }, // custom icon for RDS
  EC2: { icon: 'src/assets/ec2.png' }, // custom icon for EC2
  IP: { icon: 'src/assets/ip-icon.jpg' }, // custom icon for IP's
}
