export const resourceVisuals: Record<
  string,
  { icon?: string; color?: string }
> = {
  VPC: { icon: 'assets/vpc.png' }, // custom icon for VPC
  RDS: { icon: 'assets/rds.jpg' }, // custom icon for RDS
  EC2: { icon: 'assets/ec2.png' }, // custom icon for EC2
  IP: { icon: 'assets/ip-icon.jpg' }, // custom icon for IP's
}
